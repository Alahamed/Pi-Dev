{% extends 'base.html.twig' %}

{% block title %}File{% endblock %}

{% block body %}
    <h1>File</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFile</th>
                <td>{{ file.idFile }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ file.file }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ file.dateCreation ? file.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Myfile</th>
                <td>{{ file.myfile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('file_index') }}">back to list</a>

    <a href="{{ path('file_edit', {'idFile': file.idFile}) }}">edit</a>

    {{ include('file/_delete_form.html.twig') }}
{% endblock %}
